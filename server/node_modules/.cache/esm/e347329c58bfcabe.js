let express,dotenv,mongoose,cors,cookieParser;_58b‍.w("express",[["default",["express"],function(v){express=v}]]);_58b‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_58b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_58b‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_58b‍.w("cookie-parser",[["default",["cookieParser"],function(v){cookieParser=v}]]);
const app = express();







// dotenv
dotenv.config();

// connect databse
mongoose.connect(process.env.MONGO_URI)
    .then(() => {
        console.log("database connected successfully")
    })
    .catch((err) => {
        _58b‍.g.console.log("error while connecting to databse => ", err)
    })

// middlewares
app.use(cors({
    origin: process.env.CLIENT_URL
}));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

const port = process.env.PORT || 8080;

app.listen(port, () => {
    console.log(`server is running on port ${port}`);
})