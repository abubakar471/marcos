let express,dotenv,mongoose,cors,cookieParser;_206‍.w("express",[["default",["express"],function(v){express=v}]]);_206‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_206‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_206‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_206‍.w("cookie-parser",[["default",["cookieParser"],function(v){cookieParser=v}]]);





const app = express();

// dotenv
dotenv.config();

// connect databse
mongoose.connect(process.env.MONGO_URI)
    .then(() => {
        console.log("database connected successfully")
    })
    .catch((err) => {
        _206‍.g.console.log("error while connecting to databse => ", err)
    })

// middlewares
app.use(cors({
    origin: process.env.CLIENT_URL
}));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

const port = process.env.PORT || 8080;

app.listen(port, () => {
    console.log(`server is running on port ${port}`);
})